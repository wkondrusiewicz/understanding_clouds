#!/usr/bin/env python
from setuptools import setup, find_packages
setup(
    name='understanding_clouds',
    version='0.1',
    description='Repository for predicting and classyfing masks for satelite cloud images',
    packages=find_packages(include=['understanding_clouds']),
    install_requires=[
    ],
)
